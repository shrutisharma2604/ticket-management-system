# Copy to backend/.env and provide local values. Never commit backend/.env.
# OPERATOR_PASSWORD must contain a BCrypt password hash, not plaintext.
# FRONTEND_ORIGIN must be a single origin (not *). Default: http://localhost:5173
JWT_SECRET=
OPERATOR_USERNAME=
OPERATOR_PASSWORD=
